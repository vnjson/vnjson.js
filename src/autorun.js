
import catalog from './catalog';

function autorun(){
	/*catalog.map((obj)=>{
		if(obj.hasOwnProperty('autorun')){
			if(obj.autorun===true){
				
				obj.handler(obj.event);
			}
		}
		
	});*/
};


export default autorun;